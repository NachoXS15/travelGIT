export interface categoryProp{
    categoryProp: string
}

export interface PackageProps{
    id: string
    destino: string,
    Descripcion?: string,
    salida: string,
    categoria: string,
    imgUrl: string
}
